ul.nav.nav-tabs.nav-justified

    li class="#{'active' if current_page?(user_path(@user))}"
      =link_to user_path(@user) do 
        span.badge
          =@user.bookshelves.count
        |本棚
    li class="#{'active' if current_page?(records_user_path(@user))}"
      =link_to records_user_path(@user) do 
        span.badge
          =@user.records.count
        |記録
    li class="#{'active' if current_page?(microposts_user_path(@user))}"
      =link_to microposts_user_path(@user) do 
        span.badge
          =@user.microposts.count
        |投稿
    li class="#{'active' if current_page?(likes_user_path(@user))}"
      =link_to likes_user_path(@user) do 
        span.badge
          =@user.likes.count
        |お気に入り
    li class="#{'active' if current_page?(following_user_path(@user))}"
      =link_to following_user_path(@user) do
        span.badge
          = @user.following.count
        |フォロー
    li class="#{'active' if current_page?(followers_user_path(@user))}"
      =link_to followers_user_path(@user) do
        span.badge
          = @user.followers.count
        |フォロワー
